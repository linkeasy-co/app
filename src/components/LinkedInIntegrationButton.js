import React from 'react';

const LinkedInIntegrationButton = ({ onClick }) => {
  return (
    <button onClick={onClick} className="btn-linkedin">
      Integrar com LinkedIn
    </button>
  );
};

export default LinkedInIntegrationButton;
